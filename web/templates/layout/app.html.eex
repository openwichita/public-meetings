<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <meta name="description" content="A list of Wichita Public Meetings">
    <meta name="author" content="Open Wichita">

    <title>Wichita Public Meetings</title>
    <link rel="stylesheet" href="<%= static_path(@conn, "/css/app.css") %>">
  </head>

  <body>
    <div class="container">
      <header class="header">
        <nav role="navigation">
          <ul class="nav nav-pills pull-right">
            <li><a href="/">Home</a></li>
            <li><a href="/meetings">Meetings</a></li>
            <%= if @current_user == nil do %>
              <li><%= link "Login", to: session_path(@conn, :new) %></li>
              <li><%= link "Register", to: registration_path(@conn, :new) %></li>
            <%= else %>
              <li><%= link "Logout", to: session_path(@conn, :delete), method: :delete %></li>
            <% end %>
          </ul>
        </nav>
        <h3 class="brand"><a id="mainLogo" href="/">Wichita Public Meetings</a></h3>
      </header>

      <p class="alert alert-info" role="alert"><%= get_flash(@conn, :info) %></p>
      <p class="alert alert-danger" role="alert"><%= get_flash(@conn, :error) %></p>

      <main role="main">
        <%= render @view_module, @view_template, assigns %>
      </main>

    </div> <!-- /container -->
    <script src="<%= static_path(@conn, "/js/app.js") %>"></script>
  </body>
  
  <footer>
    <p>
      Supported by <a href="http://openwichita.com/" target="_blank">Open Wichita</a>.
    </p>
  </footer>
</html>
