<h2>Edit meeting type</h2>

<%= render "form.html", changeset: @changeset,
                        action: meeting_type_path(@conn, :update, @meeting_type) %>

<%= link "Back", to: meeting_type_path(@conn, :index) %>
