<h2>New meeting type</h2>

<%= render "form.html", changeset: @changeset,
                        action: meeting_type_path(@conn, :create) %>

<%= link "Back", to: meeting_type_path(@conn, :index) %>
