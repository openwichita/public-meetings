<h2>Edit meeting date</h2>

<%= render "form.html", changeset: @changeset,
                        action: meeting_date_path(@conn, :update, @meeting_date) %>

<%= link "Back", to: meeting_date_path(@conn, :index) %>
