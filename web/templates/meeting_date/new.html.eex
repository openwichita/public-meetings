<h2>New meeting date</h2>

<%= render "form.html", changeset: @changeset,
                        action: meeting_date_path(@conn, :create) %>

<%= link "Back", to: meeting_date_path(@conn, :index) %>
